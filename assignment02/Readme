To build the program, please run
>> make all


Updates:
  type inference

Caution:
  The inference of tuple of unknown length is supported.
  The type inference succeeds for all the files in the TEST folder except for one file TEST/list_opr.stfpl. (segment fault)
  This is acceptable since list_opr.stfpl actually requires a list not a tuple.

