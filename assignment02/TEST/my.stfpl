/*
** computing the factorial numbers
*/

let
  // val
  // rec a = b
  // and b = a
  val c = 9
  val f = fix f (x) => if x > 1 then f (x - 1) else 1
  // val f: int -> int = fix f (x: int): int => if x > 1 then f (x - 1) else 1
  val g = lam (x) => 3
  // val g: (int) -> int = lam (x: int): int => 3

  val x = g (3)
  // val y = f (3)
  // val _ = print_int a
  // val _ = print_int b
  // val _ = print_int c
in
  3  // ()
end // end of [let]

/* ****** ****** */

/* end of [fact.stfpl] */
